{
    IBClasses = (
        {
            ACTIONS = {
                getEditBuffer = id; 
                getPatch = id; 
                openPreferences = id; 
                sendPatch = id; 
                storeCancelAction = id; 
                storeOKAction = id; 
                storePatch = id; 
            }; 
            CLASS = Controller; 
            LANGUAGE = ObjC; 
            OUTLETS = {bankNumber = id; mStoreSheet = id; patchNumber = id; }; 
            SUPERCLASS = NSObject; 
        }, 
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }
    ); 
    IBVersion = 1; 
}